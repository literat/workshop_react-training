{
  "name": "nextjs",
  "private": true,
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "deps": "rimraf node_modules package-lock.json flow-typed && npm install && flow-typed install",
    "flow": "flow"
  },
  "dependencies": {
    "next": "^5.0.1-canary.2",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "flow-bin": "^0.65.0",
    "flow-typed": "^2.3.0",
    "rimraf": "^2.6.2"
  }
}
